<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--前面是标准开头-->
<mapper namespace="com.example.demo.mapper.DBMapper">
    <select id="findAll" resultType="com.example.demo.entity.Inner">
        SELECT * FROM depend
    </select>


    <select id="queryByModule" resultType="com.example.demo.entity.Outer">
        SELECT * FROM outer_depend
    </select>
</mapper>